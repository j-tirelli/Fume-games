{
  "name": "database",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "database": "sequelize-cli db:create",
    "unDB": "sequelize-cli db:migrate db:drop",
    "migrate": "sequelize-cli db:migrate",
    "unmigrate": "sequelize-cli db:migrate:undo:all",
    "seed": "sequelize-cli db:seed:all --debug",
    "unseed": "sequelize-cli db:seed:undo:all",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "faker": "^5.1.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^2.2.5",
    "sequelize": "^6.3.5",
    "sequelize-cli": "^6.2.0"
  }
}
